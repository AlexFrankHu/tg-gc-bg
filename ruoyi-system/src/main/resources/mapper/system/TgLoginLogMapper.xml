<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.TgLoginLogMapper">

    <resultMap type="TgLoginLog" id="TgLoginLogResult">
        <result property="id"          column="id"           />
        <result property="phone"       column="phone"        />
        <result property="result"      column="result"       />
        <result property="reason"      column="reason"       />
        <result property="tgUserId"    column="tg_user_id"   />
        <result property="nickname"    column="nickname"     />
        <result property="proxyInfo"   column="proxy_info"   />
        <result property="loginTime"   column="login_time"   />
        <result property="createTime"  column="create_time"  />
        <result property="nodeId"          column="node_id"          />
    </resultMap>

    <select id="selectList" parameterType="TgLoginLog" resultMap="TgLoginLogResult">
        select * from tg_login_log
        <where>
            <if test="phone != null and phone != ''">AND phone like concat('%', #{phone}, '%')</if>
            <if test="result != null and result != ''">AND result = #{result}</if>
        </where>
        order by id desc
    </select>

</mapper>
